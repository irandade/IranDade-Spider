[project]
name = "irandade-spider"
version = "0.1.0"
description = "Scrapy spider to crawl sites and download PDF/XLSX files"
requires-python = ">=3.12"

[project.scripts]
spider = "irandade_spider.main:entry"

[dependency-groups]
dev = [
    "scrapy>=2.11",
    "typer>=0.12",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
